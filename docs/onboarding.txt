Onboarding questionnaire (synced from Drive)

Notes defaults only: #1 monthly grocery budget 400; #2 every other week;
#5 3 meals, 2 snacks; #6 Monday–Saturday.
(Known-calories input still defaults to 2000 for UX; no longer a Notes item.)

Input order:
1. Monthly grocery budget
2. Grocery cadence (Weekly / every other week / monthly)
3. Daily caloric goal — known path OR Help me find out (Weight, Weight goal lose/maintain/gain, Activity)
4. What is your weight goal? — do not ask if already given in Help me find out (known path only; macros need it)
5. Meals per day (2m1s, 3 meals, 3m2s, 4 meals, 5 meals) — default 3m2s
6. Days per week checkboxes — default Mon–Sat

REMOVED: dedicated macronutrient targets question / Custom macros UI.
Macros derived automatically from weight goal:
  lose → 33 p | 42 c | 25 f
  maintain → 28 p | 42 c | 30 f
  gain → 25 p | 45 c | 30 f
(default maintain if missing)

Output: target|actual side-by-side pies; meal list with curated cals + per-ingredient macros;
reroll; nutrition 3–5 sentences; collapsed micro pies + hover + sodium note; grocery list;
Save & close / return to dashboard. Macro targets noted as following weight goal.
